{
  "schemaVersion": 1,
  "id": "week-sql-01",
  "title": "What the Database Does",
  "milestone": "sql-01-what-the-database-does",
  "track": "sql",
  "intro": "If you write SQL that works but have never looked at what the engine does with it, this is the layer underneath. Everything runs against PostgreSQL 16 — the engine most likely to be under whatever you build next, and one of the few that will hand you a raw page and tell you what is on it. A container and a psql prompt is the whole setup.",
  "days": [
    {
      "id": "sql-day-01",
      "day": 1,
      "title": "A table is a file of pages",
      "estMinutes": 25,
      "teaser": "Your UPDATE didn't update anything, and VACUUM gave back none of the 4 GB",
      "teachBack": "A page is the unit of I/O, and an UPDATE writes a new version of the row rather than editing it. Put those two facts together and explain why a table can triple in size while its row count stays flat — and what plain VACUUM does about it, given that it does not shrink the file.",
      "slug": "a-table-is-pages",
      "status": "available",
      "theoryMarkdown": "You already write queries that work. This track is about the layer underneath them: what\nthe engine does with the bytes once your `SELECT` arrives. Start with the most physical\nquestion there is — where does a row actually live?\n\nEverything below is run against **PostgreSQL 16**, because it is the engine most likely\nto be under whatever you build next, and because it is unusually willing to show you its\nown internals. Stack Overflow's developer survey has had it as the most-used database\nsince 2023, and by 2025 it had pulled to 55.6% of all respondents — 58.2% of professional\ndevelopers, roughly eighteen points clear of MySQL in second. But popularity is not why\nit earns this track. It earns it because you can ask Postgres to hand you a raw 8 kB page\nand tell you what is on it, and almost nothing else will.\n\n### A row is not the unit of anything\n\nThe mental model most people carry is that a table is a list of rows, and reading a row\nreads a row. Neither half is true.\n\nA table on disk is a flat array of fixed-size **pages** — Postgres calls the file a\n*heap*. Every page is the same size, and the page is the unit the engine reads, writes,\ncaches and locks. Rows are packed into pages; a page holds as many as fit. When you ask\nfor one row, the engine reads the whole page that row sits in, because that is the\nsmallest thing it can read.\n\n```sql\nSHOW block_size;\n```\n\n```\n block_size\n------------\n 8192\n```\n\n8192 bytes, and it is a compile-time constant: changing it means rebuilding Postgres and\nrecreating every cluster, which is why essentially nobody does. SQLite defaults to 4096,\nmatching the virtual-memory page and most filesystem blocks. The number is a compromise\nnobody has wanted to relitigate since the 1990s — bigger pages waste less space on\nper-page bookkeeping and read more useful bytes per seek, smaller pages waste less I/O\nwhen you only wanted one row.\n\nWatch the file grow in page-sized steps rather than row-sized ones:\n\n```sql\nCREATE TABLE t(id bigserial PRIMARY KEY, name text, note text);\nSELECT pg_relation_size('t') / 8192 AS pages;\nINSERT INTO t(name, note) SELECT 'name' || g, repeat('x', 40) FROM generate_series(1, 2000) g;\nSELECT pg_relation_size('t') / 8192 AS pages, pg_size_pretty(pg_relation_size('t')) AS heap;\n```\n\n```\n pages\n-------\n     0\n\n pages |  heap\n-------+--------\n    23 | 184 kB\n```\n\nA freshly created Postgres table is **zero pages** — an empty file. (SQLite's answer is\ndifferent and worth knowing: creating a table there costs pages before any row exists,\nbecause the schema and the table's own tree root have to live somewhere.) Then 2000 rows\nland in 23 pages, and the file grew one page at a time at moments that had nothing to do\nwith your row count. Ninety-seven of those rows fit on the first page — a number you will\nbe able to derive rather than guess by the end of this lesson.\n\n### Where a row is, exactly\n\nEvery Postgres row carries hidden system columns you never declared. The one that\nanswers \"where does this row live\" is `ctid`:\n\n```sql\nSELECT ctid, id, name FROM t WHERE id IN (1, 2, 82) ORDER BY id;\n```\n\n```\n  ctid  | id |  name\n--------+----+--------\n (0,1)  |  1 | name1\n (0,2)  |  2 | name2\n (0,82) | 82 | name82\n```\n\n`(page, slot)`. Row 1 is the first slot of page zero; row 82 is the 82nd slot of the same\npage. This is a **physical address**, not an identity — and the difference is the whole\nlesson of the next section.\n\n### An UPDATE does not update\n\nHere is the fact that surprises people who have used Postgres for years:\n\n```sql\nUPDATE t SET note = 'changed' WHERE id = 1;\nSELECT ctid FROM t WHERE id = 1;\n```\n\n```\n  ctid\n---------\n (22,56)\n```\n\nThe row moved from page 0 to page 22. Postgres did not modify the row in place. It wrote\na **new version** of the row wherever there was space, and marked the old one as dead.\nBoth copies are in the file right now.\n\nThat is MVCC — multi-version concurrency control — and it is the mechanism behind\nPostgres's most-loved property: readers never block writers and writers never block\nreaders, because a reader is looking at a version that hasn't gone anywhere. The two\nother hidden columns are the bookkeeping:\n\n```sql\nSELECT ctid, xmin, xmax, id FROM t ORDER BY id LIMIT 3;\n```\n\n```\n  ctid   | xmin | xmax | id\n---------+------+------+----\n (22,56) |  798 |    0 |  1\n (0,2)   |  797 |    0 |  2\n (0,3)   |  797 |    0 |  3\n```\n\n`xmin` is the transaction that created this version, `xmax` the transaction that deleted\nit (0 if it is still live). Row 1 wears its update on its sleeve: same table, same\n`INSERT`, but a *newer* `xmin` than its neighbours, because the version you are looking\nat was created by the `UPDATE` and not by the insert. Every query you run is really \"show me the versions visible\nto my transaction id\", which is why a long-running `SELECT` can sit there returning a\nconsistent snapshot of a table being rewritten underneath it.\n\nThe bill arrives as **bloat**:\n\n```sql\nSELECT pg_size_pretty(pg_relation_size('t')) AS before_updates;\nUPDATE t SET note = note || 'a';\nSELECT count(*) AS live_rows, pg_size_pretty(pg_relation_size('t')) AS file FROM t;\n```\n\nRun that middle statement three times, changing the appended character each time, so\nevery row gets three new versions:\n\n```\n live_rows |  file\n-----------+--------\n      2000 | 720 kB\n```\n\nSame 2000 rows, and the file went from 184 kB to 720 kB. Three updates of every row left\nthree dead versions of every row lying in the heap. Now clean up:\n\n```sql\nVACUUM t;\nSELECT pg_size_pretty(pg_relation_size('t')) AS after_vacuum;\nVACUUM FULL t;\nSELECT pg_size_pretty(pg_relation_size('t')) AS after_vacuum_full;\n```\n\n```\n after_vacuum\n--------------\n 720 kB\n\n after_vacuum_full\n-------------------\n 184 kB\n```\n\n**Plain `VACUUM` gave back nothing.** It marks the dead space reusable *by that table*,\nso the file stops growing — but it does not shrink the file, and this is the single most\nmisunderstood thing about Postgres operations. `VACUUM FULL` genuinely returns the space,\nby rewriting the whole table into a new file, and it takes an `ACCESS EXCLUSIVE` lock for\nthe duration: nothing reads or writes that table until it finishes. On a large production\ntable that is an outage, which is why teams reach for `pg_repack` instead, and why \"our\ndisk is full and `VACUUM` didn't help\" is a support conversation that happens every week\nof the year.\n\n> [!NOTE]\n> **The most expensive consequence of ctid, in production.** Because an index entry\n> points at a physical `(page, slot)`, moving a row means every index on the table has to\n> be updated — even indexes on columns your `UPDATE` never touched. Uber's engineering\n> blog made this famous in 2016 as their headline reason for migrating off Postgres:\n> a table with a dozen indexes turned a one-column update into twelve index writes, and\n> that amplification then rode through their replication stream, which ships physical\n> page changes rather than statements. MySQL's InnoDB avoids it by making secondary\n> indexes point at the primary key instead of a physical location — which costs an extra\n> lookup on every index read. There is no free answer here, only a trade someone picked.\n\n### Worth knowing: ask Postgres for the raw bytes of a page\n\nThere is a contrib extension that reads pages directly, and it turns every claim above\ninto something you can check yourself. Rebuild the table first if you have been updating\nit — the numbers below are page 0 of a table freshly loaded with those 2000 rows:\n\n```sql\nCREATE EXTENSION IF NOT EXISTS pageinspect;\nSELECT lower, upper, pagesize FROM page_header(get_raw_page('t', 0));\nSELECT count(*) AS tuples, min(lp_len) AS small, max(lp_len) AS big FROM heap_page_items(get_raw_page('t', 0));\n```\n\n```\n lower | upper | pagesize\n-------+-------+----------\n   412 |   432 |     8192\n\n tuples | smallest | largest\n--------+----------+---------\n     97 |       79 |      80\n```\n\nDo the arithmetic and the page layout falls out of it. A page starts with a 24-byte\nheader, then grows an array of 4-byte **line pointers** downward from the front while the\nrow data itself is stacked upward from the back. `lower` is where the pointer array ends,\n`upper` where the data begins, and the gap between them is the free space — here\n432 − 412 = 20 bytes, so this page is full.\n\nWhich gives you an invariant to check rather than a number to believe:\n**`lower` = 24 + 4 × tuples**. Here that is 24 + 97 × 4 = 412, exactly. Run those two\nqueries against any page of any table of your own and it will still hold, whatever the\ncounts happen to be. That layer of indirection is also why a row can be moved *within* a\npage without any index noticing — the line pointer keeps the slot number stable while the\ndata behind it moves.\n\nNow account for one row's 79 bytes. Eight for the bigint, six for `'name1'`, 41 for the\n40-character note — varying-length values each carry a length byte — and the other 24 are\na per-row header that is yours whether you want it or not. Postgres charges 23 bytes of\nvisibility bookkeeping per row, rounded up for alignment. That is why a table of millions\nof narrow rows is far bigger than `rows × columns` suggests, and why \"just add a boolean\ncolumn\" is never as free as it sounds.\n\n> [!NOTE]\n> **The clock nobody watches until it stops them.** Transaction ids are 32-bit, so they\n> wrap. Postgres protects itself by refusing all writes once a table's oldest unfrozen\n> transaction gets too far behind — `SELECT name, setting FROM pg_settings WHERE name =\n> 'autovacuum_freeze_max_age'` shows the threshold, 200 million by default, against a\n> 2^31 ceiling. Vacuum's *other* job, besides reclaiming space, is freezing old rows so\n> this never happens. When it falls behind on a write-heavy table the ending is always\n> the same: Sentry's engineering blog describes losing most of a US working day to\n> exactly this, and the only recovery is to stop accepting writes and vacuum.",
      "quiz": [
        {
          "id": "q1",
          "prompt": "A table has 200 rows packed into 6 pages. A query returns exactly one row, found by scanning. What is the smallest amount of data the engine could have read from disk?",
          "options": [
            {
              "text": "One page — 8 kB in PostgreSQL",
              "correct": true,
              "why": "The page is the unit of I/O. Even a single-byte column in a single row costs one whole page, because a page is the smallest thing the engine can ask the filesystem for."
            },
            {
              "text": "Just the bytes of that row",
              "correct": false,
              "why": "Nothing in the stack can read a partial page. The row's bytes arrive inside the page that holds them; you never pay per row."
            },
            {
              "text": "All six pages, since a scan reads the whole table",
              "correct": false,
              "why": "A scan *may* read every page, but it can stop the moment it has what it needs. The question asks for the smallest possible, and that is the one page the row sits on."
            },
            {
              "text": "It depends entirely on the row's width",
              "correct": false,
              "why": "Width changes how many rows fit per page, not the size of a read. A 20-byte row and a 2000-byte row both cost one page to reach."
            }
          ]
        },
        {
          "id": "q2",
          "prompt": "You run `UPDATE t SET note = 'x' WHERE id = 1` in PostgreSQL. What happens to the row's physical location, and to the previous version of the row?",
          "options": [
            {
              "text": "The row's bytes are overwritten where they sit, so the ctid is unchanged",
              "correct": false,
              "why": "That would break the property Postgres is built around: a concurrent reader holding an older snapshot must still be able to see the previous version. It cannot, if you overwrote it. The old version has to survive the update."
            },
            {
              "text": "The old version is deleted immediately and the new one takes its slot",
              "correct": false,
              "why": "Immediate deletion has the same problem — a transaction that started before your update is entitled to keep reading the old version. Reclaiming that space is a *later*, separate job, which is exactly what VACUUM is for."
            },
            {
              "text": "The row moves only if the new value is longer than the old one",
              "correct": false,
              "why": "Length is irrelevant to whether a new version is written; a new version is always written. It can influence *where* the new version lands, but even shrinking a value produces a second copy."
            },
            {
              "text": "A new version of the row is written elsewhere — possibly on a different page — and the old version stays in the file, marked dead",
              "correct": true,
              "why": "This is MVCC. An UPDATE is an insert plus a tombstone, never an edit in place. Watch `ctid` change across the statement and you have seen it happen; the dead version is still on disk until a vacuum reclaims its space."
            }
          ]
        },
        {
          "id": "q3",
          "prompt": "Your table's file is 4 GB on disk, of which only 500 MB is live rows. You run `VACUUM`. What happens to the size of the file, and why?",
          "options": [
            {
              "text": "It stays 4 GB, and the dead space is permanently wasted until the table is dropped",
              "correct": false,
              "why": "Half right, and the wrong half matters. The space is not wasted: it is on the free space map and the next inserts and updates will land in it. That is why a well-vacuumed table reaches a steady size instead of growing forever."
            },
            {
              "text": "It stays 4 GB — VACUUM marks the dead space reusable by that table but does not return it to the operating system",
              "correct": true,
              "why": "This is the most misunderstood thing in Postgres operations. Plain VACUUM stops the file from growing further, because new rows fill the freed space, but the file only shrinks if free space happens to sit at the very end. To actually get the 3.5 GB back you need VACUUM FULL, which rewrites the table into a new file under an ACCESS EXCLUSIVE lock — or pg_repack, which does it without the lock."
            },
            {
              "text": "It drops to roughly 500 MB, since that is what is still live",
              "correct": false,
              "why": "That is VACUUM FULL's behaviour, not VACUUM's, and the distinction matters precisely when a disk is filling up. Plain VACUUM never rewrites the file."
            },
            {
              "text": "It grows, because VACUUM writes bookkeeping about what it freed",
              "correct": false,
              "why": "VACUUM does write — it touches the visibility map and the free space map — but those are separate small forks, not growth of the table's own file."
            }
          ]
        },
        {
          "id": "q4",
          "prompt": "In PostgreSQL an index entry points at a physical `(page, slot)` address. Given only that, why can updating one *unindexed* column of a row cause writes to five different indexes?",
          "options": [
            {
              "text": "It cannot — Postgres skips indexes whose columns did not change",
              "correct": false,
              "why": "There is an optimisation in this direction, HOT (heap-only tuple) updates, but it only applies when the new version fits on the *same page*. Once the row has to move to another page, every index must be updated."
            },
            {
              "text": "Each index stores a copy of the whole row, so all copies must be kept in sync",
              "correct": false,
              "why": "An index stores its own key columns plus a pointer, not the whole row. If it stored the whole row, updating an indexed column would be the expensive case — but here the expensive case is updating an *unindexed* one."
            },
            {
              "text": "Postgres rebuilds all indexes on a table whenever any row in it changes",
              "correct": false,
              "why": "Nothing so drastic — the work is per-row, not per-table. A single-row update adds one entry per index, which is bad enough at scale but is not a rebuild."
            },
            {
              "text": "The update writes the row at a new physical address, so every index that points at the old address has to be given the new one",
              "correct": true,
              "why": "The indexes are not stale because of what the column meant — they are stale because of where the row *is*. This write amplification was Uber's headline reason for migrating off Postgres in 2016. InnoDB dodges it by having secondary indexes point at the primary key rather than a location, paying an extra lookup on every index read instead."
            }
          ]
        },
        {
          "id": "q5",
          "prompt": "What are the hidden `xmin` and `xmax` columns on a PostgreSQL row, and how do they let one transaction read a table while another rewrites it?",
          "options": [
            {
              "text": "A read lock and a write lock held on the row while a transaction touches it",
              "correct": false,
              "why": "Backwards — these fields exist so that locks are *not* needed for reading. Postgres's readers-never-block-writers property comes precisely from resolving visibility by comparing ids rather than by taking a lock."
            },
            {
              "text": "The transaction that created this row version and the one that deleted it — so each transaction can filter to the versions that were live when it started",
              "correct": true,
              "why": "Visibility is a comparison, not a lock. Your transaction has a snapshot; a row version is visible if its xmin committed before that snapshot and its xmax did not. Since every writer creates new versions rather than editing old ones, the versions your snapshot needs are still sitting there."
            },
            {
              "text": "Timestamps recording when the row was inserted and last modified",
              "correct": false,
              "why": "Tempting, and they are used for ordering — but they are transaction ids, not times. That distinction is why they can run out: they are 32-bit counters, and the ceiling of about two billion is what makes wraparound a real operational hazard."
            },
            {
              "text": "The minimum and maximum values of the row's indexed columns",
              "correct": false,
              "why": "They have nothing to do with your data. The x stands for transaction (xact); both hold transaction ids, and they are the same on a row whose columns are entirely unrelated."
            }
          ]
        }
      ],
      "task": {
        "markdown": "Get a PostgreSQL server in front of you and make it show you its own pages. Do not take\nany of the numbers above on trust — the point of this exercise is that you produced them.\n\nIf you have no Postgres to hand, `docker run --rm -e POSTGRES_PASSWORD=x -p 5432:5432\npostgres:16` and `psql -h localhost -U postgres` is a 30-second setup.\n\n1. Confirm `block_size`, then create a table and check `pg_relation_size` before and\n   after inserting a couple of thousand rows. Record where the page count jumps.\n2. Select `ctid` alongside your own columns and work out how many rows are landing per\n   page.\n3. Update one row. Select its `ctid` again. Explain the number you get.\n4. Update *every* row three times, then compare `pg_relation_size` and `count(*)`.\n5. Run `VACUUM`, measure again, then `VACUUM FULL`, and measure again. Only one of them\n   changes the file size — record both numbers.\n\nWrite every number you saw into the file as a comment beside the query that produced it.\nThat file is the point of the exercise, not the queries.",
        "files": [
          "sql/day01.sql"
        ],
        "compile": "psql -h localhost -U postgres -f sql/day01.sql",
        "checklist": [
          "`block_size` confirmed and the table's page count measured before and after inserts",
          "`ctid` selected, and rows-per-page worked out from it",
          "a single row observed changing its `ctid` after an `UPDATE`",
          "the file measured growing while `count(*)` stayed the same",
          "`VACUUM` and `VACUUM FULL` both measured, and you can say why only one shrank it"
        ]
      }
    },
    {
      "id": "sql-day-02",
      "day": 2,
      "title": "An index is a sorted copy",
      "estMinutes": 30,
      "teaser": "Same index, same query, one word different — and the planner refuses it",
      "teachBack": "An index is a second sorted tree, not an annotation on the table. Use that to explain both why `WHERE lower(email) = ?` cannot use an index on `email`, and why a planner that has a perfectly usable index will sometimes choose to ignore it and be right to.",
      "slug": "an-index-is-a-sorted-copy",
      "status": "available",
      "theoryMarkdown": "Yesterday: a table is a heap of pages, and finding a row without help means reading all\nof them. Today: what an index actually is, and the four separate reasons a perfectly good\none gets ignored.\n\n### It is a second table you don't get to see\n\nAn index is not an annotation on a table, and it is not a hint. It is **another B-tree,\ncontaining the indexed columns plus a pointer back to the row's physical location**, kept\nsorted.\n\nThat single sentence explains almost everything indexes do:\n\n- Lookups get fast, because the tree is sorted — finding a value is a descent through\n  three or four pages instead of a walk through all of them.\n- Writes get slower, because every `INSERT` now writes two trees, and (as yesterday's\n  `ctid` lesson showed) an `UPDATE` that moves a row writes *every* index on the table.\n- The database gets bigger, sometimes much bigger, because the index is a real copy of\n  real data.\n- `ORDER BY` on the indexed column can become free — the sorted order already exists on\n  disk, so there is nothing to sort.\n\nThat third point is not theoretical. Two indexes on a 50,000-row table:\n\n```\n  relname  | pg_size_pretty\n-----------+----------------\n idx_lower | 1544 kB\n idx_name  | 1544 kB\n s         | 4552 kB\n```\n\nTwo indexes on one column each, and they are already two thirds of the size of the table\nholding three columns. This is why index bloat is a real budget line and why\n`pg_stat_user_indexes.idx_scan = 0` — an index nobody has ever used — is the first thing\na Postgres consultant grep's for.\n\nYou never pay for an index once. You pay on every write, forever, in exchange for the\nreads.\n\n### Ask the planner, never guess\n\nEvery engine will tell you its plan. In Postgres:\n\n```sql\nCREATE TABLE s(id bigserial PRIMARY KEY, name text, note text);\nINSERT INTO s(name, note) SELECT 'name' || g, repeat('x', 40) FROM generate_series(1, 50000) g;\nANALYZE s;\nEXPLAIN SELECT * FROM s WHERE name = 'name7';\n```\n\n```\n                     QUERY PLAN\n-----------------------------------------------------\n Seq Scan on s  (cost=0.00..1194.00 rows=1 width=58)\n   Filter: (name = 'name7'::text)\n```\n\n`Seq Scan` — every page, to find one row. Now build the index:\n\n```sql\nCREATE INDEX idx_name ON s(name);\nANALYZE s;\nEXPLAIN SELECT * FROM s WHERE name = 'name7';\n```\n\n```\n Index Scan using idx_name on s  (cost=0.29..8.31 rows=1 width=58)\n   Index Cond: (name = 'name7'::text)\n```\n\nCost 1194 to cost 8.31. Note `ANALYZE` in there — not `EXPLAIN ANALYZE`, the *other*\n`ANALYZE`, which updates the planner's statistics. Postgres does not pick plans by\nreading your query; it picks them by estimating rows from a sample of your data. Stale\nstatistics are behind an enormous share of \"it was fast yesterday\" incidents, and it is\nthe first thing to check after a bulk load.\n\nTwo numbers, both estimates: `cost` is in arbitrary units where 1.0 is roughly one\nsequential page read, and `rows` is how many the planner *thinks* will come back.\n`EXPLAIN ANALYZE` runs the query and prints what really happened next to the guess — and\nthe gap between estimate and reality is where nearly every planner mystery is solved.\n\n### Reason one: you asked about a different value\n\nHere is the rule that catches everyone: **an index on a column can only answer questions\nabout that column's value.** Wrap the column in a function and you are asking about a\ndifferent value, which the sorted copy is not sorted by.\n\n```sql\nEXPLAIN SELECT * FROM s WHERE lower(name) = 'name7';\n```\n\n```\n Seq Scan on s  (cost=0.00..1319.00 rows=250 width=58)\n   Filter: (lower(name) = 'name7'::text)\n```\n\n`Seq Scan`, with `idx_name` sitting right there. Same story for `WHERE price * 1.2 > 100`,\n`WHERE date(created_at) = '2026-01-01'`, and every `WHERE CAST(id AS text) = ?` written to\nmake a type error go away. Postgres's answer is to let you index the expression itself:\n\n```sql\nCREATE INDEX idx_lower ON s(lower(name));\nANALYZE s;\nEXPLAIN SELECT * FROM s WHERE lower(name) = 'name7';\n```\n\n```\n Index Scan using idx_lower on s  (cost=0.29..8.31 rows=1 width=58)\n   Index Cond: (lower(name) = 'name7'::text)\n```\n\nExpression indexes are a genuine Postgres advantage — MySQL only got them in 8.0.13, in\n2018 — and the case-insensitive-email index is the single most common use of them in\nproduction schemas anywhere.\n\n### Reason two: the index would be slower\n\nThis is the one people refuse to believe, so watch it happen. One table, one index, one\nquery shape — only the *value* changes:\n\n```\n=== WHERE status = 'rare'      (2,000 of 200,000 rows)\n Index Only Scan using idx_status on t2 (actual time=0.033..1.644 rows=2000 loops=1)\n Execution Time: 1.788 ms\n\n=== WHERE status = 'common'    (198,000 of 200,000 rows)\n Parallel Seq Scan on t2 (actual time=0.016..13.148 rows=99000 loops=2)\n Execution Time: 24.699 ms\n```\n\nThe planner used the index for `'rare'` and *refused* it for `'common'`, and it was right\nboth times. An index scan costs one descent per matching row, plus a jump to the heap for\neach; once you are fetching most of the table, reading it straight through in physical\norder is cheaper than 198,000 random jumps. This is why \"add an index\" does nothing for\nyour `WHERE deleted = false` filter when 99% of rows are not deleted — and why a partial\nindex (`CREATE INDEX ... WHERE deleted = true`) is often the actual answer.\n\n### Reason three: the query needed a column the index doesn't carry\n\nIf the index contains every column the query needs, the engine can answer from the index\nalone and never touch the heap:\n\n```sql\nEXPLAIN SELECT name FROM s WHERE name = 'name7';\n```\n\n```\n Index Only Scan using idx_name on s  (cost=0.29..8.31 rows=1 width=9)\n```\n\n**`Index Only Scan`** is the phrase to look for — it means the second lookup, index to\nrow, did not happen. Change that `SELECT name` to `SELECT *` and it drops back to a plain\n`Index Scan`, because `note` only exists in the heap. This is why adding one more column\nto an existing index sometimes beats adding a whole new index, and Postgres has dedicated\nsyntax for exactly that: `CREATE INDEX ... INCLUDE (note)` carries the extra column in the\nindex's leaf pages without making it part of the sort key.\n\n> [!NOTE]\n> Index-only scans in Postgres are only *mostly* index-only. The index has no visibility\n> information, so the engine still has to know whether each row is visible to your\n> transaction — it consults the **visibility map**, one bit per page saying \"everything on\n> this page is visible to everyone\". Pages the map hasn't marked force a heap fetch\n> anyway. `EXPLAIN ANALYZE` prints `Heap Fetches:`, and a high number there on a\n> supposedly index-only scan almost always means the table needs vacuuming. Two lessons\n> in and vacuum is already the answer to a second unrelated question.\n\n### Worth knowing: `C.UTF-8` is not `C`, and the difference is a sequential scan\n\nA prefix search looks like it should be a range scan — everything starting with `name7`\nis contiguous in a sorted list. It isn't:\n\n```sql\nEXPLAIN SELECT * FROM s WHERE name LIKE 'name7%';\n```\n\n```\n Seq Scan on s  (cost=0.00..1194.00 rows=1010 width=58)\n   Filter: (name ~~ 'name7%'::text)\n```\n\nThe reason is **collation**. An index is sorted according to some collation, and a\nlocale-aware one (`en_US.UTF-8`, and yes `C.UTF-8`) can sort in ways where the strings\nmatching a prefix are not one contiguous run — so `LIKE 'x%'` cannot be turned into a\nrange. Only the `C` collation, which compares byte by byte, guarantees it can.\n\nThe trap is that `C.UTF-8` *looks* like `C` and is not. Prove it by declaring an index\nwith the real thing:\n\n```sql\nCREATE INDEX idx_c ON s(name COLLATE \"C\");\nANALYZE s;\nEXPLAIN SELECT * FROM s WHERE name LIKE 'name7%';\n```\n\n```\n Bitmap Heap Scan on s  (cost=26.44..624.27 rows=1010 width=58)\n   ->  Bitmap Index Scan on idx_c  (cost=0.00..26.19 rows=990 width=0)\n         Index Cond: ((name >= 'name7'::text) AND (name < 'name8'::text))\n```\n\nLook at what the planner did with your `LIKE`: it rewrote it into `name >= 'name7' AND\nname < 'name8'`, a range, exactly as the intuition said it should. The equivalent without\ntouching collation is a `text_pattern_ops` index, which sorts by byte regardless of the\ndatabase's locale.\n\nTwo more things worth carrying out of this. First, the plan is a **Bitmap Heap Scan**, not\nan Index Scan — Postgres's middle gear for when a few thousand rows match: collect all\ntheir locations first, sort them into physical order, then sweep the heap once. Second,\nand this is the part that bites in production: a database created with a locale-aware\ncollation will *silently* not use your index for prefix searches, and you will discover it\nthe week after it worked on someone's laptop where the default was different.",
      "quiz": [
        {
          "id": "q1",
          "prompt": "You add an index to speed up a slow report. Name two distinct costs you have just taken on.",
          "options": [
            {
              "text": "Every write to the table now maintains a second tree, and the index occupies real disk space of its own",
              "correct": true,
              "why": "Those are the two standing costs, and neither is small. Two single-column indexes on a three-column 50,000-row table came to 1544 kB each against a 4552 kB table — two thirds of the table's size, for two columns. And in PostgreSQL the write cost is worse than it first looks: because index entries hold a physical row location, an UPDATE that moves a row must touch every index on the table, not just the ones on columns you changed."
            },
            {
              "text": "Queries that do not use the index have to skip past it while scanning",
              "correct": false,
              "why": "An index is a separate file. A sequential scan of the table never reads it and never steps over it."
            },
            {
              "text": "The table must be locked whenever the index is used",
              "correct": false,
              "why": "Reading through an index takes no special lock. There is a locking cost to *building* one — which is why CREATE INDEX CONCURRENTLY exists — but that is a one-off, not a cost of having the index."
            },
            {
              "text": "Reads of other columns get slower, because the row is now split in two",
              "correct": false,
              "why": "The row is not split. The index is an additional copy of the indexed columns; the heap still holds every column exactly as before, and reads that ignore the index are unaffected."
            }
          ]
        },
        {
          "id": "q2",
          "prompt": "A query filters on an indexed column and the plan still shows `Seq Scan`, even though the index exists and the column is right. Give two different reasons that can happen.",
          "options": [
            {
              "text": "The column is nullable, and PostgreSQL cannot index columns that contain NULLs",
              "correct": false,
              "why": "PostgreSQL B-trees do index NULLs, and `WHERE col IS NULL` can use an index. (This is a real difference between engines — Oracle's B-trees famously omit all-NULL keys — but it is not why Postgres would choose a Seq Scan.)"
            },
            {
              "text": "The filter wraps the column in a function or a cast, or the value matches so many rows that scanning is genuinely cheaper",
              "correct": true,
              "why": "Two entirely different failures that produce the identical symptom. The first is a correctness limit — an index on `name` is sorted by `name`, so it can say nothing about `lower(name)`. The second is the planner being right: fetching 198,000 of 200,000 rows through an index means 198,000 random heap jumps, and reading the table straight through beat it by more than 10x in the lesson's measurement."
            },
            {
              "text": "The index has not finished building, or the table has too few rows for an index to exist",
              "correct": false,
              "why": "An unfinished CREATE INDEX is simply not visible to the planner yet, so this is not really a case of an index being ignored. And no row-count threshold governs whether an index can exist — small tables just rarely benefit."
            },
            {
              "text": "The statistics are stale, which makes the index physically unusable",
              "correct": false,
              "why": "Stale statistics are a genuine and very common cause of a bad plan — but they mislead the planner's *estimate*, they do not make an index unusable. Run ANALYZE and the same index may suddenly be chosen for the same query."
            }
          ]
        },
        {
          "id": "q3",
          "prompt": "What does `Index Only Scan` mean in a PostgreSQL plan, what work does it save, and what can still force it to touch the heap anyway?",
          "options": [
            {
              "text": "The index carried every column the query needed, so the heap lookup was skipped — except for pages the visibility map has not marked all-visible",
              "correct": true,
              "why": "The saving is the second lookup: index entry to row. The catch is that an index holds no visibility information, so Postgres must still establish that each row is visible to your transaction. It uses the visibility map, one bit per page, and any page not marked forces a heap fetch after all. EXPLAIN ANALYZE prints `Heap Fetches:` — a large number there usually means the table wants vacuuming."
            },
            {
              "text": "Only one index was used, rather than combining several",
              "correct": false,
              "why": "Combining indexes is a real Postgres capability, but it shows up as a BitmapAnd or BitmapOr over Bitmap Index Scans. \"Index Only\" refers to not visiting the heap."
            },
            {
              "text": "The query was answered from the index because the table itself was locked by another transaction",
              "correct": false,
              "why": "Plan choice has nothing to do with locks held elsewhere. The planner picks this shape purely because the index happens to contain every column required."
            },
            {
              "text": "The whole index was loaded into memory, so no disk reads were needed",
              "correct": false,
              "why": "\"Only\" describes which structures were consulted, not where they live. An Index Only Scan still reads index pages from disk if they are not cached."
            }
          ]
        },
        {
          "id": "q4",
          "prompt": "Your table has an index on `email`. Which of `WHERE email = ?`, `WHERE lower(email) = ?` and `WHERE email LIKE 'a%'` can use it, and what decides the last one?",
          "options": [
            {
              "text": "Equality and `LIKE 'a%'`; `lower(email)` works too once the statistics are refreshed",
              "correct": false,
              "why": "ANALYZE cannot help here. There is no entry anywhere in an index on `email` that records what `lower(email)` is, so no amount of better estimation makes that index answer the question. You need a second index, on the expression."
            },
            {
              "text": "Only the equality one; B-trees answer equality and nothing else",
              "correct": false,
              "why": "B-trees are excellent at ranges — that is what being sorted buys you. `>`, `<`, `BETWEEN` and `ORDER BY` all use them, and a prefix `LIKE` is a range in disguise when the collation permits."
            },
            {
              "text": "All three, since all three mention only the indexed column",
              "correct": false,
              "why": "Mentioning the column is not the test. The test is whether the query's condition can be expressed as a range over the *order the index is sorted in* — which `lower(email)` cannot be, at any collation."
            },
            {
              "text": "Equality yes; `lower(email)` no, without an index on the expression; `LIKE 'a%'` only if the index's collation is `C` or it uses `text_pattern_ops`",
              "correct": true,
              "why": "The first two follow from the index being sorted by `email` and nothing else. The third is the subtle one: turning `LIKE 'a%'` into the range `>= 'a' AND < 'b'` is only valid if the sort order is bytewise. A locale-aware collation may interleave strings so that the prefix matches are not contiguous — and `C.UTF-8` counts as locale-aware, despite the name."
            }
          ]
        },
        {
          "id": "q5",
          "prompt": "`EXPLAIN` says a query will return 3 rows; `EXPLAIN ANALYZE` shows it actually returned 40,000. What is most likely wrong, and what does the planner do with a bad estimate?",
          "options": [
            {
              "text": "Nothing is wrong; `EXPLAIN` reports rows examined and `EXPLAIN ANALYZE` reports rows returned",
              "correct": false,
              "why": "Both report the same quantity: rows emitted by that node. One is the planner's estimate and the other is the measured truth, which is exactly why comparing them is useful."
            },
            {
              "text": "The query changed between the two runs, so the numbers are not comparable",
              "correct": false,
              "why": "EXPLAIN ANALYZE plans and executes the same statement, so the estimate and the actual come from one run. They are directly comparable by design."
            },
            {
              "text": "The planner notices the discrepancy mid-execution and re-plans with the real number",
              "correct": false,
              "why": "PostgreSQL does not re-plan mid-execution; the plan is fixed before the first row is produced, and a bad estimate is lived with to the end. Adaptive re-planning of this kind exists in some other systems, but assuming it here is how people conclude a plan \"should have fixed itself\"."
            },
            {
              "text": "The statistics are stale or unrepresentative — and since the estimate is what chooses the plan, a wrong one picks a plan suited to a query that does not exist",
              "correct": true,
              "why": "This gap is where nearly every planner mystery is solved, which is why you reach for EXPLAIN ANALYZE rather than EXPLAIN. Believing three rows, the planner happily picks an index scan or a nested loop; at 40,000 rows those become 40,000 random jumps or 40,000 inner scans. ANALYZE after a bulk load is the first thing to try, and correlated columns — where the planner assumes independence and is wrong — are the next suspect."
            }
          ]
        }
      ],
      "task": {
        "markdown": "Prove each of these to yourself rather than taking the lesson's word for it. Use enough\nrows that the planner has a real decision to make — 50,000 is plenty, and\n`generate_series` will produce them in one statement.\n\n1. `EXPLAIN` an equality filter with no index. Record the plan and its cost.\n2. Add the index, `ANALYZE`, run it again, and record how both changed.\n3. Wrap the column in `lower()` and watch it fall back to a sequential scan.\n4. Add an expression index and watch it recover.\n5. Get one query to report `Index Only Scan`, then change it so the same index gives you\n   a plain `Index Scan`, and work out what you changed.\n6. Build a column where one value is rare and one is common, index it, and find the query\n   where Postgres uses the index and the query where it refuses. Use `EXPLAIN ANALYZE` and\n   compare the execution times — the refusal should be the faster one.\n\nWrite every plan you saw into the file as a comment beside the query that produced it.\nThat file is the point of the exercise, not the queries.",
        "files": [
          "sql/day02.sql"
        ],
        "compile": "psql -h localhost -U postgres -f sql/day02.sql",
        "checklist": [
          "the same query planned with and without an index, both plans and costs recorded",
          "a function on the indexed column observed falling back to a sequential scan",
          "an expression index built, and the plan recovering",
          "one query reporting `Index Only Scan`, and one reporting `Index Scan` off the same",
          "the planner observed *refusing* an index for an unselective value, with"
        ]
      }
    },
    {
      "id": "sql-day-03",
      "day": 3,
      "title": "NULL is not a value",
      "estMinutes": 25,
      "teaser": "The query that returned zero rows for two years and never logged an error",
      "teachBack": "Walk through why `x NOT IN (1, 2, NULL)` returns nothing for every possible `x`. Expand the expression, say what each term evaluates to, and explain why `WHERE` then drops the row — including why it drops it silently rather than erroring.",
      "slug": "null-is-not-a-value",
      "status": "available",
      "theoryMarkdown": "Two lessons about where bytes live. This one is about a single value that isn't one, and\nthe class of bug it produces: queries that are syntactically perfect, throw no error, log\nnothing, and return the wrong answer for years.\n\n### It is not zero, and it is not empty\n\n`NULL` means *unknown*. Not absent, not blank, not a default — unknown. Once you take\nthat literally, everything else follows, including the parts that look broken.\n\nIf a value is unknown, then any comparison with it is also unknown:\n\n```sql\nSELECT NULL = NULL AS \"null = null\", NULL <> NULL AS \"null <> null\", NULL IS NULL AS \"is null\";\n```\n\n```\n null = null | null <> null | is null\n-------------+--------------+---------\n             |              | t\n```\n\nThe first two came back **blank — which is psql printing NULL**, not false. `NULL = NULL`\nis not false; it is unknown, because two unknown quantities may or may not be equal and\nSQL declines to guess. This is why `IS NULL` exists as separate syntax: it is the only\noperator that asks about the unknown-ness itself rather than about the value.\n\nSo SQL does not run on booleans. It runs on **three-valued logic**: true, false, unknown.\nAnd the rule that turns that from trivia into production bugs is one line long:\n\n> `WHERE` keeps a row only when the condition is **true**. Unknown is discarded, exactly\n> like false.\n\nSilently. That is the whole failure mode.\n\n### The query that returns nothing, forever\n\nHere is the shape, and it is in more codebases than anyone would like:\n\nThree users, and one of the two orders has a NULL `user_id` — a returns row, a deleted\naccount, a legacy import, whatever your schema permits:\n\n```sql\nCREATE TABLE users(id int, name text);\nINSERT INTO users VALUES (1, 'ana'), (2, 'bo'), (3, 'cy');\n```\n\n```sql\nCREATE TABLE orders(id int, user_id int);\nINSERT INTO orders VALUES (10, 1), (11, NULL);\nSELECT name FROM users WHERE id NOT IN (SELECT user_id FROM orders);\n```\n\n```\n name\n------\n(0 rows)\n```\n\nTwo users have never ordered anything. The query says nobody has. It is a perfectly\nreasonable-looking way to ask the question, it returns a perfectly reasonable-looking\nempty result, and it is wrong.\n\nExpand it for user 2. `2 NOT IN (1, NULL)` means `2 <> 1 AND 2 <> NULL`:\n\n```\n 2=1 | 2=NULL | not in\n-----+--------+--------\n f   |        |\n```\n\n`2 <> 1` is true. `2 <> NULL` is **unknown**. And `true AND unknown` is unknown — not\ntrue — so `WHERE` drops the row. Now notice that nothing about this depended on the\nnumber 2. Whatever value you test, the second term stays unknown, so the whole `NOT IN`\nis at best unknown and the row is always dropped. **A single NULL anywhere in the list\nmakes `NOT IN` return nothing for every possible input.**\n\n`IN` is not symmetric with it: `2 IN (1, NULL)` is also unknown and also drops the row,\nbut at least it can return true when there *is* a match, so the bug hides only the misses.\n`NOT IN` hides everything.\n\nThe fix is `NOT EXISTS`, which is written in terms of rows rather than values and so has\nnothing to be unknown about:\n\n```sql\nSELECT name FROM users WHERE NOT EXISTS (SELECT 1 FROM orders o WHERE o.user_id = users.id);\n```\n\n```\n name\n------\n bo\n cy\n```\n\nPostgres will also usually plan `NOT EXISTS` better — as an anti-join rather than as a\nfilter — so the correct version is generally the faster one too. That is rare enough to\nbe worth enjoying.\n\n### It is contagious\n\nUnknown propagates through arithmetic and string operations as well as comparisons:\n\n```sql\nSELECT 'a' || NULL AS concat, 1 + NULL AS arith, coalesce('a' || NULL, 'fallback') AS fixed;\n```\n\n```\n concat | arith |  fixed\n--------+-------+----------\n        |       | fallback\n```\n\n`'a' || NULL` is not `'a'`. If one part of the greeting you are building is unknown, the\ngreeting is unknown. Every templating bug that renders an empty string where a name\nshould be traces back to this, and `coalesce(x, fallback)` — return the first non-NULL\nargument — is the standard defence.\n\nAggregates go the other way and **skip NULLs entirely**, which is the source of a whole\nsecond family of quiet wrongness:\n\n```sql\nCREATE TABLE n(v int);\nINSERT INTO n VALUES (1), (2), (NULL);\nSELECT count(*), count(v), sum(v), avg(v) FROM n;\n```\n\n```\n count | count | sum |       avg\n-------+-------+-----+--------------------\n     3 |     2 |   3 | 1.5000000000000000\n```\n\n`count(*)` counts rows: 3. `count(v)` counts non-NULL values: 2. And `avg` is 1.5, not 1\n— it divided by 2, not by 3. If those NULLs represent \"sensor was offline\", the average\nyou just reported is the average of the readings you have, which is a defensible answer.\nIf they represent \"we forgot to record a zero\", it is a lie. The database cannot tell the\ndifference, and neither can the dashboard.\n\n### The inconsistency the experts actually argue about\n\nHere is where SQL stops being merely counter-intuitive and becomes genuinely\nself-contradictory. `WHERE v = NULL` matches nothing, because two unknowns are not known\nto be equal. Put a second NULL into that same table and ask two more questions of it:\n\n```sql\nINSERT INTO n VALUES (NULL);\nSELECT v, count(*) FROM n GROUP BY v ORDER BY v;\nSELECT count(DISTINCT v) AS distinct_v, count(*) AS all_rows FROM n;\n```\n\n```\n v | count            distinct_v | all_rows\n---+-------           ------------+----------\n 1 |     1                     2 |        4\n 2 |     1\n   |     2\n```\n\n`GROUP BY` collected the two NULL rows into **one group of two** — treating them as equal\nto each other, in a language where `NULL = NULL` is not true — while `count(DISTINCT v)`\nignored them entirely and reported 2 distinct values out of 4 rows. `ORDER BY` has an opinion too:\nPostgres sorts NULLs as larger than every value, so they land last ascending and first\ndescending, and `ORDER BY x NULLS FIRST` exists because the standard left it to the\nimplementation.\n\nSo NULLs are unequal in `WHERE`, equal in `GROUP BY` and `DISTINCT`, absent from\naggregates, and last-by-default in `ORDER BY`. Four different answers to \"are two unknowns\nthe same thing\", inside one language.\n\nThat inconsistency is why this is one of the longest-running arguments in database theory.\nC. J. Date, who has spent four decades writing about the relational model and worked with\nCodd himself, argues that nulls should be removed from it outright: that three-valued\nlogic makes queries mean things their authors cannot predict, and that missing information\nbelongs in additional relations rather than in a magic value. It is not a fringe position,\nand it is not a settled one — Claude Rubinson's *Nulls, three-valued logic, and ambiguity\nin SQL* (SIGMOD Record, 2007) argues Date misreads his own example queries, while\nconceding the broader point: nulls introduce a startling amount of complexity into\napparently straightforward queries. Nobody in that argument thinks the current situation\nis *good*. You have to write queries in it anyway.\n\n### Worth knowing: UNIQUE lets in as many NULLs as you like — until you ask it not to\n\nA `UNIQUE` constraint is defined in terms of equality, and NULLs are never equal, so:\n\n```sql\nCREATE TABLE u(v int UNIQUE);\nINSERT INTO u VALUES (NULL), (NULL), (NULL);\nSELECT count(*) FROM u;\n```\n\n```\n count\n-------\n     3\n```\n\nThree NULLs in a unique column, no error. This is the standard's behaviour and it is\ncorrect by the rules — but it is not what \"unique\" means to the person reading your\nschema, and it is why partial unique indexes and `NOT NULL` on key columns are so common.\n\nPostgreSQL 15 finally added the other option:\n\n```sql\nCREATE TABLE u2(v int UNIQUE NULLS NOT DISTINCT);\nINSERT INTO u2 VALUES (NULL);\nINSERT INTO u2 VALUES (NULL);\n```\n\n```\nERROR:  duplicate key value violates unique constraint \"u2_v_key\"\nDETAIL:  Key (v)=(null) already exists.\n```\n\n`NULLS NOT DISTINCT` says: for the purpose of this constraint, treat unknowns as equal to\neach other. It arrived roughly thirty years into the language's life, which tells you how\nlong people put up with the alternative.\n\nOne more tool worth carrying: `IS DISTINCT FROM` is `<>` with unknowns treated as\ncomparable, so `WHERE v IS DISTINCT FROM 1` returns the NULL rows as well as the 2s —\nwhich is almost always what the person writing `WHERE v <> 1` actually meant.",
      "quiz": [
        {
          "id": "q1",
          "prompt": "In PostgreSQL, what does `NULL = NULL` evaluate to, and what does `WHERE` do with a row whose condition evaluates to that?",
          "options": [
            {
              "text": "False, and the row is discarded",
              "correct": false,
              "why": "The row is discarded, but the reason matters enormously. If it were false you could rely on `NOT (NULL = NULL)` being true — and it is not, it is unknown too. That is precisely how NOT IN goes wrong."
            },
            {
              "text": "True, since both sides are identical",
              "correct": false,
              "why": "Identical *syntax*, not identical values. NULL is not a value that can be equal to anything, including another NULL — which is why `IS NULL` had to be invented as separate syntax."
            },
            {
              "text": "Unknown, a third truth value — and `WHERE` discards the row, exactly as it would for false",
              "correct": true,
              "why": "NULL means unknown, so whether two unknowns are equal is itself unknown. SQL runs on three-valued logic, and `WHERE` keeps a row only when the condition is *true*. Because unknown and false are discarded identically and silently, a query built on unknowns returns a plausible-looking result and logs nothing."
            },
            {
              "text": "It raises an error, which is why `IS NULL` exists",
              "correct": false,
              "why": "No error is raised, and that is the whole danger. An error would be a gift; you would find it in a log. Instead the expression quietly evaluates to unknown and the row silently vanishes."
            }
          ]
        },
        {
          "id": "q2",
          "prompt": "Walk through why `WHERE x NOT IN (1, 2, NULL)` returns no rows for every possible value of `x`.",
          "options": [
            {
              "text": "`NOT IN` cannot be used with subqueries that return NULL, so PostgreSQL returns an empty set",
              "correct": false,
              "why": "Nothing special-cases NULL here and nothing is refused. The empty result is the ordinary evaluation of ordinary three-valued logic, which is exactly why it is so hard to spot."
            },
            {
              "text": "It expands to `x <> 1 AND x <> 2 AND x <> NULL`; the last term is always unknown, so the whole conjunction is at best unknown and never true",
              "correct": true,
              "why": "The crucial part is that this does not depend on x. Even when x is 7 and both real comparisons are true, `true AND true AND unknown` is unknown, and `WHERE` keeps only true. One NULL anywhere in the list disables the entire predicate for every input. `NOT EXISTS` is the fix, because it asks about the existence of rows rather than the equality of values."
            },
            {
              "text": "The NULL matches every value, so every row is excluded by the NOT",
              "correct": false,
              "why": "A tidy story that gives the right answer for the wrong reason, and it will mislead you next time. NULL matches nothing — the comparison yields unknown, not a match. If NULL genuinely matched everything, plain `IN` would return every row, and it returns none."
            },
            {
              "text": "The list is sorted for the lookup and NULL sorts before everything, truncating it",
              "correct": false,
              "why": "No sorting or truncation is involved. `IN` against a literal list is a chain of equality comparisons; the outcome comes from what those comparisons evaluate to."
            }
          ]
        },
        {
          "id": "q3",
          "prompt": "A column `v` holds the values 1, 2 and NULL. What do `count(*)`, `count(v)` and `avg(v)` return, and why can that quietly make a dashboard wrong?",
          "options": [
            {
              "text": "An error, since an average cannot be computed over an unknown value",
              "correct": false,
              "why": "No error. Aggregates over columns containing NULLs are entirely routine — the risk is a plausible number, not a failure. (`avg` over *no* non-NULL rows at all returns NULL rather than raising.)"
            },
            {
              "text": "3, 2 and NULL — one unknown input makes the whole aggregate unknown",
              "correct": false,
              "why": "That is how NULL behaves in *scalar* expressions — `1 + NULL` really is NULL — but aggregates deliberately break that rule and skip nulls instead. Knowing that aggregates are the exception is most of the point of this question."
            },
            {
              "text": "3, 2 and 1.5 — aggregates skip NULLs, so the average divided by 2 rather than 3",
              "correct": true,
              "why": "`count(*)` counts rows, `count(v)` counts non-NULL values, and `avg` sums 3 over 2 values. Whether 1.5 is right depends entirely on what the NULL meant. \"Sensor offline\" makes it the correct average of known readings; \"we failed to record a zero\" makes it a 50% overstatement. The database cannot distinguish the two cases, and neither can anything downstream of it."
            },
            {
              "text": "3, 3 and 1.0 — NULL counts as zero in numeric aggregates",
              "correct": false,
              "why": "This is the intuition the behaviour is designed to contradict. NULL is not zero; it is unknown. Treating it as zero would mean claiming knowledge you do not have, so SQL leaves it out of the calculation entirely."
            }
          ]
        },
        {
          "id": "q4",
          "prompt": "Name a place where SQL treats two NULLs as equal to each other, and a place where it treats them as not equal.",
          "options": [
            {
              "text": "Equality depends on the column's type; numeric NULLs compare equal, text NULLs do not",
              "correct": false,
              "why": "Type has nothing to do with it. The behaviour differs by *clause* — GROUP BY versus WHERE — and is identical for every data type."
            },
            {
              "text": "`GROUP BY` and `DISTINCT` put all NULLs together; `WHERE v = NULL` and a plain `UNIQUE` constraint treat them as never equal",
              "correct": true,
              "why": "Both halves are real and they contradict each other inside one language. Two NULL rows collapse into a single group, yet `WHERE v = NULL` matches neither of them, and a UNIQUE column will happily accept both. `ORDER BY` takes a third position — Postgres sorts NULLs as larger than everything, hence NULLS FIRST as an option. This inconsistency is what the long-running theoretical argument about nulls is actually about."
            },
            {
              "text": "They are equal everywhere except inside aggregate functions",
              "correct": false,
              "why": "Backwards on the main point, and aggregates are not doing an equality comparison at all — they simply exclude NULL inputs before computing anything."
            },
            {
              "text": "They are never treated as equal anywhere; that is the single consistent rule",
              "correct": false,
              "why": "It would be a much easier language if that were true. Run `GROUP BY v` on a table with two NULL rows and you get one group of two — which is only possible if something decided those NULLs were the same."
            }
          ]
        },
        {
          "id": "q5",
          "prompt": "A `UNIQUE` column in PostgreSQL already contains a NULL. You insert another NULL. What happens, and how would you make it fail instead?",
          "options": [
            {
              "text": "It is accepted — uniqueness is defined by equality and NULLs are never equal — and `UNIQUE NULLS NOT DISTINCT` makes the second one fail",
              "correct": true,
              "why": "You can insert as many NULLs into a UNIQUE column as you like, which is correct by the rules and almost never what the person reading the schema expects. PostgreSQL 15 added NULLS NOT DISTINCT to say \"for this constraint, treat unknowns as equal\"; before that the options were NOT NULL or a partial unique index. It took about thirty years for the language to offer the choice."
            },
            {
              "text": "It is accepted, but only because the column is nullable; a NOT NULL column would compare NULLs as equal",
              "correct": false,
              "why": "A NOT NULL column cannot contain a NULL at all, so there is nothing there to compare. Nullability governs whether NULLs may be stored, never how they compare once stored."
            },
            {
              "text": "It is rejected — a unique constraint permits at most one NULL",
              "correct": false,
              "why": "That is SQL Server's behaviour, and assuming it is portable is how this bug crosses engines. The SQL standard, and PostgreSQL with it, allows any number of NULLs in a unique column by default."
            },
            {
              "text": "It is accepted, and there is no way to forbid it short of `NOT NULL`",
              "correct": false,
              "why": "Right about the acceptance, out of date on the remedy. NOT NULL is one answer, but `UNIQUE NULLS NOT DISTINCT` (PostgreSQL 15+) forbids the duplicate NULL while still permitting a single one — and a partial unique index solved it before that."
            }
          ]
        }
      ],
      "task": {
        "markdown": "Build the bug, then fix it, then prove the fix.\n\n1. Create a `users` table and an `orders` table where `orders.user_id` is nullable, and\n   insert at least one order whose `user_id` is NULL.\n2. Ask \"which users have never ordered\" with `NOT IN` and a subquery. Record the result.\n   It should be visibly, silently wrong.\n3. Expand the failing condition by hand for one specific user: select each term of the\n   `AND` separately and record what each one evaluates to.\n4. Rewrite the question with `NOT EXISTS` and record the correct result.\n5. Then explore the inconsistency: on a table containing two NULLs, run `GROUP BY`,\n   `count(DISTINCT ...)`, `ORDER BY` and a `WHERE v = NULL`, and record which of them\n   treated the two NULLs as the same thing.\n6. Finally, get a `UNIQUE` column to accept several NULLs, then get it to reject them.\n\nWrite every result you saw into the file as a comment beside the query that produced it.\nThe comparison of step 2 against step 4 is the point of the exercise.",
        "files": [
          "sql/day03.sql"
        ],
        "compile": "psql -h localhost -U postgres -f sql/day03.sql",
        "checklist": [
          "`NOT IN` with a NULL in the list observed returning zero rows",
          "the failing condition expanded term by term, with the unknown one identified",
          "the same question answered correctly with `NOT EXISTS`",
          "`count(*)`, `count(v)` and `avg(v)` compared on a column containing a NULL",
          "a `UNIQUE` column observed accepting several NULLs, then rejecting them"
        ]
      }
    }
  ]
}
